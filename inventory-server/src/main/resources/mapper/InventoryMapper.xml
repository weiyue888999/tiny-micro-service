<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.xiaomo.inventory.mapper.InventoryMapper">

    <select id="findByItemId" resultType="io.xiaomo.inventory.entity.Inventory">
        SELECT * FROM t_inventory where item_id = #{item_id}
    </select>

    <insert id="insert" parameterType="io.xiaomo.inventory.entity.Inventory">
        insert into
        t_inventory(item_id,create_time,modified_time,cnt,version)
        values(#{itemId},#{createTime},#{modifiedTime},#{cnt},0)
    </insert>

    <update id="update">
        update t_inventory set cnt = #{cnt} and version = version+1 where version = #{version} and item_id = #{itemId}
    </update>

    <update id="incressCnt">
        update t_inventory set cnt = cnt - #{cnt} and version = version+1 where version = #{version} and item_id = #{itemId} and cnt > 0
    </update>

    <update id="descresCnt">
        update t_inventory set cnt = cnt + #{cnt} and version = version+1 where version = #{version} and item_id = #{itemId}
    </update>

</mapper>